<header>
    <h1> {{ shop.name }}</h1>
    <nav>
        <ul class="navigation">
            {% for link in linklists.main-menu.links %}
                <li class="menu">{{ link.title | link_to: link.url }}            
            {% endfor %}
        </ul>
    </nav>
</header>