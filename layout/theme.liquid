<html>
    <head>
        {{ content_for_header }}
        {{ 'style.css' | asset_url | stylesheet_tag }}
        {{ 'custom.css' | asset_url | stylesheet_tag }}
    </head>
    <body>
        
        {% render 'header' %}
            <main class="content">
                {{ content_for_layout }}
            </main>
        {{ 'app.js' | asset_url | script_tag }}
        
    </body>
</html>
